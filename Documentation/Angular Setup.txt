Navigate to the SchedulerApp directory:
cd SchedulerApp

Install npm:
npm install

Run:
npx ng serve

To install libraries:
       # Library name
ng add @angular/material

production build to dist directory:
ng build --configuration=production